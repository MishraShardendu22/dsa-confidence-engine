id: clone_graph
title: Clone Graph
description: "Given a reference of a node in a connected undirected graph, return a deep copy (clone) of the graph. Each node contains a value (int) and a list of neighbors. For simplicity, we represent the graph as an adjacency list where graph[i] is a list of nodes adjacent to node i (1-indexed). Return the adjacency list of the cloned graph."
language: python
entrypoint: solve
entrypoint_aliases:
  - cloneGraph
  - clone_graph
starter_code: |
  def solve(adj):
      # adj is an adjacency list (list of lists, 1-indexed by node value)
      # Return cloned adjacency list as a list of sorted neighbor lists
      pass
tests:
  - id: "1"
    input: "{\"adj\": [[2, 4], [1, 3], [2, 4], [1, 3]]}"
    expected_output: "[[2, 4], [1, 3], [2, 4], [1, 3]]"
  - id: "2"
    input: "{\"adj\": [[]]}"
    expected_output: "[[]]"
  - id: "3"
    input: "{\"adj\": []}"
    expected_output: "[]"
  - id: "4"
    input: "{\"adj\": [[2], [1, 3], [2]]}"
    expected_output: "[[2], [1, 3], [2]]"
accepted_strategies:
  - dfs_graph_clone
  - bfs_graph_clone
required_concepts: []
optional_concepts:
  - dfs
  - bfs
  - hashmap
primary_concepts:
  - dfs
  - hashmap
