id: subsets
title: Subsets
description: "Given an integer array nums of unique elements, return all possible subsets (the power set). The solution set must not contain duplicate subsets. Return the solution in any order, but sort each subset and sort the list of subsets."
language: python
entrypoint: solve
entrypoint_aliases:
  - subsets
starter_code: |
  def solve(nums):
      # Write your solution here
      pass
tests:
  - id: "1"
    input: "{\"nums\": [1, 2, 3]}"
    expected_output: "[[], [1], [1, 2], [1, 2, 3], [1, 3], [2], [2, 3], [3]]"
  - id: "2"
    input: "{\"nums\": [0]}"
    expected_output: "[[], [0]]"
  - id: "3"
    input: "{\"nums\": [1, 2]}"
    expected_output: "[[], [1], [1, 2], [2]]"
accepted_strategies:
  - backtracking_subsets
  - iterative_bitmask_subsets
required_concepts: []
optional_concepts:
  - backtracking
  - recursion
primary_concepts:
  - backtracking
